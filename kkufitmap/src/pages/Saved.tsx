import { IonContent, IonHeader, IonPage, IonTitle, IonToolbar } from '@ionic/react';

const Home: React.FC = () => {
    return (
        <IonPage>
            <IonHeader>
                <IonContent fullscreen>
                Save Page
                </IonContent>
            </IonHeader>
        </IonPage>
    );
};

export default Home;
